# This app intentionally uses Django's built-in auth.User model.
# If you later need extra profile fields (organization, role, etc.),
# add a OneToOne "Profile" model here and a signal to auto-create it.
